# 对于boundary的设计，先假设boundary是个cube.
#用list of list, list里面装boudary的最大值和最小值

#要解决一个函数在一个空间内的最值
#输入一个函数。一个boundary，返回一个最值


import random



global gBest

def f(x):
    return 2-x[0]**2



class particle:

    def __init__(self, v, pos, pBest, ):
        self.v = c1 * v + c2 *random.random()*
        self.pos =
        self.pBest =
        # 记住自己的纬度
        self.dimension =
        self.


    def move(self, ):
        self = self + v


    def evaluate(self, f):
        # Evaluate position < boundary?   velocity < Vmax?







    #用函数传输函数

    def g(a, b):
        return a*b

    def f(a, b):
        return a+b


    #最终的hardcoded的pso

    def pso(f, boundary):
        initialize particles

        for p in particles:

        for p in particles:

        click for end